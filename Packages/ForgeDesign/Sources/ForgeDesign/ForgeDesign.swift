import SwiftUI

// Phase 4A placeholder. Concrete tokens (Typography, Spacing, Radius,
// Palette, Materials, Surfaces, ViewModifiers) land in Phase 4E alongside
// the app shell that consumes them.
//
// This file exists so SPM treats the target as having at least one source.
public enum ForgeDesignPackage {
    public static let version = "0.4.0-phase4a"
}
